# Local reference material (contains links, brainstorming notes)
misc/

# Heavy / regenerable artifacts
data/**/adapter/
data/**/*.npz
data/**/sequences.jsonl
data/**/base_sequences.jsonl
data/**/trait_eval_raw.jsonl
data/**/greedy.jsonl
data/**/divergence_raw.jsonl

# Keep tracked: metadata.json, metrics.json, loss_curve.csv, train_log.jsonl, summary.parquet
!data/**/metadata.json
!data/**/metrics.json
!data/**/loss_curve.csv
!data/**/summary.parquet

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.env

# SDD scratch (ledger, task briefs, review packages, reports)
.superpowers/

# Local docs (specs, plans, runbook) — not published
docs/

# Validation runs live under data/ but are fully local (not published).
# A directory-level ignore, so metadata/metrics/summary inside are NOT re-included.
# Agent scratch (research downloads); user-curated notes live in docs/scratch_research/
.scratch_research/

data/validation*/

# Notebook builder (the emitted analysis.ipynb is the tracked artifact)
build_notebook.py
